# TODO

Retained future improvement ideas that are not active implementation permission and are not unresolved architecture decisions.

## API Consistency Ideas

- Audit existing wrappers against `architecture/API_CHECKLIST.md` in focused target-sized batches.
- Audit target dependencies against `Package.swift` and `architecture/MODULES.md` without forcing a speculative hierarchy migration.
- Improve specification references for public wrappers when those wrappers are next changed or audited.
- Record legacy naming cleanup candidates only with exact source evidence and compatibility impact.

## Documentation and Examples

- Add focused examples for event listener option and removal behavior after the corresponding APIs are verified.
- Add template usage examples only if repeated wrapper creation shows the current templates are insufficient.

## Scope Rule

Items here are ideas, not approved tasks. Promote a concrete item to `OPEN_DECISIONS.md` when an architecture choice must be resolved, or to `TASKS.md` only after the maintainer approves executable scope and completion evidence.
